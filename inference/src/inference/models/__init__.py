"""Model runner implementations.

Each module implements :class:`inference.models.base.ModelRunner` for one
family of models. Only :mod:`inference.models.graphcast` exists today; a future
1-hour-step model would add a sibling module here.
"""
